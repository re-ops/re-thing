{
 :bind "0.0.0.0"
 :port "8083"
 :password-file "src/main/resources/password_file.conf"

 :secrets #include "secrets.edn"

 :re-thing {
   :elasticsearch {:index "re-thing"}
 }

 :elasticsearch {
  :default {
     :hosts ["https://grafana-metrics:9201"]
     :auth {
       :user #ref [:secrets :elasticsearch :user]
       :pass #ref [:secrets :elasticsearch :pass]
     }
     :self? true
   }
  }
}
